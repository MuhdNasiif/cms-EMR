import 'package:flutter/material.dart';

class EmrList extends StatelessWidget {
  const EmrList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
